# incompetech-catalog runtime + test deps. Deliberately tiny: this is a
# database over one published JSON file, served by a Flask app with no
# framework in front of it. Ranges, not hard pins: lab980 boxes run whatever
# python3.11 ships.
flask>=3.0,<4.0
# httpx is used by the build only — `python -m incompetech build` fetches the
# catalogue and the page its lookup tables are checked against. The web layer
# never makes an outbound request.
httpx>=0.27,<1.0
pytest>=8.0,<10.0
